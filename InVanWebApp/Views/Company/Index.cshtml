@model IEnumerable<InVanWebApp_BO.CompanyBO>
@{
    ViewBag.Title = "Company";
    Layout = "~/Views/Shared/MainMaster.cshtml";
}

<script src="~/Scripts/jquery.unobtrusive-ajax.js"></script>
<script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>

<!DOCTYPE html>

<html>
<head>
    <title>Item</title>
</head>
<body>

    <div class="content-wrapper" runat="server" id="divPanel1">
        <div class="page-header" style="height:5px;">
            <h4 class="page-title">Company</h4>
            <nav aria-label="breadcrum">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <i class="fa fa-home"></i>&nbsp;<a href="@Url.Action("ReorderPointOfStocks","Dashb_RTWarehouseWiseStock")">Master</a>
                    </li>
                    <li class="breadcrumb-item active">Company</li>
                </ol>
            </nav>
        </div>
        <hr />
        <div class="panel-body">
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-body">
                            <a href="@Url.Action("AddCompany","Company")" class="btn btn-sm btn-primary pull-right btn-addnew" style="float: right; background: linear-gradient(85deg, #392c70, #6a005b);">
                                <span class="fa fa-plus"></span> Company
                            </a>
                            <div class="table-responsive" style="padding-top:15px;">
                                <table class="table table-striped table-bordered hover table-sm" id="tblItem">
                                    <thead>
                                        <tr>
                                            <th>@Html.DisplayName("Company Name")</th>
                                            <th>@Html.DisplayName("Contact Person")</th>
                                            <th>@Html.DisplayName("Email ID")</th>
                                            <th>@Html.DisplayName("Contact Number")</th>
                                            <th>@Html.DisplayName("Is Active")</th>
                                            <th>@Html.DisplayName("Actions")</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                <td>@Html.DisplayFor(modelItem => item.CompanyName)</td>
                                                <td>@Html.DisplayFor(modelItem => item.ContactPersonName)</td>
                                                <td>@Html.DisplayFor(modelItem => item.EmailId)</td>
                                                <td>@Html.DisplayFor(modelItem => item.ContactPersonNo)</td>
                                                <td>@Html.DisplayFor(modelItem => item.IsActive)</td>
                                                <td>
                                                    <a href=@Url.Action("EditCompany", "Company", new { ID = item.ID }) title="Edit Company">
                                                        <i class="fa fa-edit text-primary"></i>
                                                    </a> &emsp;
                                                    <a href=@Url.Action("DeleteSupplierCustomer", "Company", new { ID = item.ID }) onclick="return confirm('Are you sure want to delete?');" title="Delete Company">
                                                        <i class="fa fa-trash" aria-hidden="true" style="color:tomato"></i>
                                                    </a>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>
@Html.Raw(TempData["Success"])
@section scripts{

    <link href="https://cdn.datatables.net/1.12.1/css/jquery.dataTables.min.css" type="text/css" />
    <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.12.1/js/jquery.dataTables.js"></script>
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.12.1/css/jquery.dataTables.css">
    @*<script src="~/DataTables-1.10.18/js/jquery.dataTables.min.js"></script>
        <link href="~/DataTables-1.10.18/css/jquery.dataTables.min.css" rel="stylesheet" />*@


    <script type="text/javascript">
        $(document).ready(function () {
            $('#tblItem').dataTable().fnDestroy();
            $('#tblItem').dataTable({
                //"aLengthMenu": [[25, 50, 75, -1], [25, 50, 75, "All"]],
                "iDisplayLength": 10
            });
        });

        var table = $('#tblItem').dataTable({
            "serverSide": false,
            "filter": true,
            "pageLength": 20
            // "searching": true,
            //"bLengthChange": false,
            // "bPaginate": true
            //"pageLength": 15
        });
    </script>
}